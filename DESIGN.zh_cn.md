# Profile 领域设计

本文档是 Profile 领域模型的定稿设计，覆盖全部领域（账户、站点、社交、
内容、互动、输出）。`BUSINESS.md` 定义产品级业务规则，
`ARCHITECTURE.md` 定义技术架构；本文档将两者落地为具体的聚合、值对象
与不变量。每条决策附带理由。

文档约定：

- 所有运行时文本（日志、异常、API 错误）使用英文，本文档仅描述设计。
- 文中长度上限单位均为字符；数量上限单位均为条目。

## 1. 总体建模原则

- 领域模型与数据库实体模型相互独立，基础设施层负责映射。
  理由：`ARCHITECTURE.md` 要求 Domain 不受持久化形态影响。
- `Blog`、`Post`、`Moment` 是三个独立聚合，不得合并为通用
  `ContentItem`；`Page` 是第四个独立聚合。四者不引入共享的可变内容
  基类，仅共享语义完全一致的小型值对象。
  理由：三者（及 Page）行为演化方向不同，合并会迫使聚合携带彼此无关
  的字段；共享基类会把一方的变更泄漏给其他方。
- 每个聚合使用强类型身份：`BlogIdentity`、`PostIdentity`、
  `MomentIdentity`、`PageIdentity`、`UserIdentity` 等，内部均为全局唯一
  Guid。聚合间关系使用对应强类型身份，例如 Post 的引用不得接受
  `MomentIdentity`。
  理由：强类型身份在编译期防止跨聚合的误引用。
- 可能无限增长的互动与历史（评论、点赞、转发、收藏、书签、修订）一律
  不存放于内容聚合内部，而是独立聚合或独立关系；互动计数（评论数、
  点赞数、转发数、收藏数）均为读投影。
  理由：无界集合会破坏聚合的加载边界与一致性边界。
- `TimelineEntry` 仅是统一读投影，不是写聚合。
  理由：时间线是多种内容的查询视图，不拥有任何写不变量。
- 系统内部一律使用全局唯一 Guid 处理关联；任何用户可见的文本标识
  （路由标识、显示名称）都不进入内部关联。
  理由：用户已明确要求内部关联不依赖可变文本标识，改名不改关联。

## 2. 部署站点 Site

`Site` 表示部署级行为，每部署恰好一个：

- `Mode`：`Personal` / `Community`。两种模式共用同一套多用户 Schema，
  模式切换是非破坏性的。
- 注册策略：`Disabled` / `Invitation` / `Open`，与 `Mode` 独立建模。
- 所有者：Personal 模式使用的站点所有者账户引用。
- Personal 模式下部署根路径暴露哪个 `AccountSite` 是可配置的，不固定
  为所有者站点。
  理由：TEMP 中此点为未决项；用户决定保留配置自由度。

自定义域名当前明确排除在设计范围之外，但仅属延后而非永久排除；未来
引入时再设计域名所有权与验证规则。

## 3. 账户 Account 与基本身份

以下规则与 `BUSINESS.md` 一致，此处定稿为领域结构：

- `Account` 聚合包含：不可变 `UserIdentity`（Guid 支撑）、可变
  `StringIdentity`、一个当前 `AccountEmail`（含验证元数据）、当前
  `AccountRole`、当前 `AccountSuspension?`、当前 `AccountBan?`、
  `AccountDeletion?`。
- 昵称、头像、简介等展示信息不属于 `Account`，见 §4。
- `StringIdentity` 规则：5–64 位 ASCII；字符集 `A-Z a-z 0-9 _ .`；不得
  以 `.` 开头或结尾；禁止连续 `.`；`_` 可连续且可位于两端；保留所选
  大小写用于展示，可用性检查、唯一性与登录查找均大小写不敏感；变更后
  仅新值可登录。仅改大小写只更新展示形态，不创建预留。实质变更后的
  旧值在可配置预留期内（默认 90 天）对所有账户（含原所有者）均不可用；
  释放时间在变更时固定，之后的配置变更不影响已固定的释放时间；释放
  后任何账户可通过正常可用性流程认领该值。
- 登录输入解析仅发生在 HTTP 边界：`#<guid>` 选择 UserId，
  `@<stringId>` 选择 StringId，无前缀默认为 StringId；`@` 与 `#` 是
  纯输入前缀，绝不出现在领域值、DTO、持久化或任何内部表示中。
- 邮箱：每账户一个当前地址，可变更；不是登录标识；不全局唯一；比较与
  规范化大小写不敏感但保留原样；存储当前地址的验证信息。
- 角色：`User < Administrator < Root`；高角色仅能管理严格低于自己的
  账户；Root 之间不得互相管理；`Profile.Console` 是受信管理面，可越级
  操作 Root。限制历史与操作者身份不属于 Account 聚合，由独立审计模块
  记录。
- 停权（Suspension）：可过期或永久（`null` 即永久）、可选理由；允许
  登录；内容保持可见；阻止一切状态变更操作。
- 封禁（Ban）：可过期或永久、可选理由；禁止登录；隐藏该账户全部
  内容。
- 账户删除：可配置恢复期（默认 14 天）；请求删除时固定恢复截止时间；
  恢复期内内容保持可见；账户可登录但只能执行恢复；到期后可永久删
  除；永久删除仍永久保留 UserId、StringId 身份记录与邮箱身份记录。
  永久删除时仅永久锁定该账户的当前 StringId；历史 StringId 的既有
  预留保持其已固定的释放时间并正常释放，其保留记录不阻止之后的认领。
  理由：当前身份在删除后不可被冒用，同时后续删除不会追溯性阻止已承
  诺释放的历史身份被复用。
- 永久删除的内容处置由用户在请求删除时二选一：
  (a) 内容保留，作者显示为"已注销账户"占位；
  (b) 内容隐藏但保留数据。
  理由：用户明确要求两种模式同时受支持，选择权属账户所有者。
- 纪念账户（Memorial Account）：独立的账户终态，用于所有者去世的账
  户，由管理员/Root 设置；效果：禁止登录、内容冻结保留（不可再变更
  或互动）、资料显示纪念标识。纪念是删除的替代终态——两者互斥：已
  纪念的账户不进入删除流程，已进入删除流程的账户不可转为纪念。
  理由：用户引入纪念体系，使逝者账户可被保留而不必删除。
- 限制叠加：生效中的 Ban 优先于删除恢复（无法登录即无法自助恢复，
  内容不可见）；Suspension 与删除并存时可登录但只能恢复，内容可见。

认证使用 FIDO/WebAuthn；集群部署时挑战存储与 Data Protection 密钥
必须共享；邮件经 MailKit 由排队后台任务发送。

## 4. 账户展示、偏好与账户站点

每账户恰好各有一个以下对象：

### 4.1 AccountProfile（公开身份展示）

字段：昵称（≤64）、头像（媒体库引用）、简介（≤2048）、位置（≤128）、
个人链接（单个 URL）、横幅/背景（媒体库引用）。

理由：公开展示信息与基本账户结构分离，`BUSINESS.md` 要求昵称、头像、
简介不属于基本账户模型。

### 4.2 AccountSettings（账户级私有偏好）

字段：

- 默认可见性：Blog、Post、Moment 各自的默认 `ContentVisibility`
  （系统级默认均为 `Public`）。
- 语言偏好、时区。
- 邮件通知偏好：新评论/评论审核、新关注者、互动（点赞/转发/引用）、
  系统通知，分别可开关。
- 关注需批准开关（见 §5）。

理由：用户明确指定默认可见性等偏好存于独立的 `AccountSettings`，而非
`AccountSite` 或 `AccountProfile`；这些是私有行为偏好，不是公开站点
配置。

### 4.3 AccountSite（账户站点配置）

每账户恰好一个 `AccountSite`，以其 `OwnerId`（UserIdentity）作为稳定
域身份，不另设站点身份。字段：

- 站点标题（≤64）、站点描述（≤1024）。
- 主题/外观设置：结构化配色/外观项（主色、背景、字体等字段），非自
  由 CSS。
- 分页大小。
- 评论默认审核策略（站点级默认，内容项可覆盖，见 §11）。
- 输出开关：RSS/Atom、Sitemap、归档页、搜索的启用开关。

`AccountSite` 不含昵称、头像、简介等公开身份信息（属于
`AccountProfile`）。内容只存 `AuthorId`，不重复存账户站点身份。
理由：一账户一站点时 OwnerId 已具备唯一性；内容与站点经作者账户即可
关联。

## 5. 社交关系

### 5.1 Follow（关注）

- 关注关系的形态按账户设置：每账户通过 `AccountSettings` 的"关注需批
  准"开关选择直接关注或经批准的关注请求。
- 禁止自我关注。待处理请求仅可由目标方批准或拒绝，仅可由请求方取消。
  拒绝与取消不设冷却期，可立即再次发起请求。仅待处理请求的唯一性在
  聚合外协调。
- 关闭关注需批准时，必须显式选择待处理请求的处置：全部保持待处理，或
  全部批准。安全默认是保持待处理，因为全部批准会立即改变对历史
  Followers 内容的访问。开关关闭后，新的关注立即变为直接关注。若选择
  全部批准，由 Application 事务性地创建关注并解决请求。
- `Followers` / `MutualFollowers` 可见性受众在查询时实时判定；关注关
  系的变化立即影响历史内容的可访问性。
  理由：用户确认实时判定，避免快照受众与当前社交关系脱节。

### 5.2 Block（屏蔽）

账户可屏蔽其他账户。屏蔽效果：

- 禁止关注（已关注的自动解除）；
- 禁止评论对方内容；
- 禁止互动（点赞、转发、引用、收藏、书签）。

禁止自我屏蔽。创建屏蔽时，在一次 Application 事务中解除双向关注以及
双向待处理关注请求。解除屏蔽不会恢复上述关系或请求。

屏蔽不使内容不可见：被屏蔽者仍可见对方的 Public 内容。
理由：用户选定三项主动行为限制，未选择内容不可见。

## 6. 共享值对象

### 6.1 ContentFormat 与 ContentBody

```text
ContentFormat
  PlainText
  Markdown

ContentBody
  Source: string
  Format: ContentFormat
```

- `ContentBody` 是不可变值对象；编辑整体替换该值。
- 格式显式记录并持久化，系统绝不从内容猜测格式；编辑可同时改变来源
  与格式。
- 渲染 HTML 是派生输出，不属于领域模型。

### 6.2 ContentVisibility

```text
ContentVisibility
  Public
  Followers
  MutualFollowers
  Private
```

- Blog、Post、Moment、Page 共用此枚举；Followers 与 MutualFollowers
  是不同受众。
- 发布之后允许修改可见性。
- 默认可见性：Blog/Post/Moment 均为 `Public`，账户可在
  `AccountSettings` 中按内容类型配置自己的默认值。

### 6.3 内容块（ContentBlock）

Blog 与 Page 的正文由有序内容块集合组成；Post、Moment、评论不使用内
容块，仅使用单一 `ContentBody`。块类型：

- 文本块：内含 `ContentBody`（Markdown 或纯文本）。
- 媒体块：媒体库引用。
- 引文块：内含 `ContentBody`；与 Post/Moment 的"引用帖 Quote"是不同
  概念。
- 代码块：内含源码文本与非空语言标识。
- 分隔线块。
- 间距块：表示一个固定单位的垂直间距，不携带可配置尺寸或嵌套内容。
  其他布局块延后设计。

文本块、引文块与代码块各自最多包含 2097152 字符文本。语言标识不得
包含首尾空白。三类文本型内容块共用该规则，因为它们都承载作者输入的
文本，并采用相同的存储与渲染边界。

单篇 Blog/Page 的块数量 ≤8192。每篇内容项标签数 ≤32。
理由：用户决定长文采用区块编辑器模型；块上限取足够大的值避免实际
限制创作。

### 6.4 发布表示（Publication）

发布状态机含 `Draft`、`Scheduled`、`PendingReview`、`Published` 四个
状态，无 `Archived` 状态。合法转换为受限矩阵：

- `Draft ⇄ Scheduled`（设定/取消定时）；
- `Draft → PendingReview`（送审）；
- `PendingReview → Published`（通过）或 `PendingReview → Draft`（退
  回）；
- `Published → Draft`（取消发布，见 §6.5 的存草稿/丢弃选择）；
- `Scheduled → Published`（到期自动发布）。

未列出的转换均不合法（例如 `Published` 不得回到 `PendingReview`，
`Scheduled` 不得退回 `PendingReview`）。
理由：用户选择受限矩阵，使发布流程的每一步都有明确的入口与出口。

- 定时发布时间使用 `DateTimeOffset`。
- 已发布项记录 `FirstPublishedAt` 与 `LastPublishedAt`；取消发布保留
  两者。
  理由：用户选择同时保留首次与最近发布时间，取消发布不抹除历史。
- 不需要 Archived 生命周期状态；CMS 归档页是读投影，不依赖内容状态。
  理由：TEMP 推荐，用户确认；"归档页面"与"归档状态"是两种概念，不可
  混淆。

### 6.5 删除表示（Deletion）

发布状态与删除状态分离建模，删除对象记录：

- `DeletedAt`；
- `PurgeAt`：删除发生时固定为 `DeletedAt + 14 天`，之后不可更改。

规则：

- 软删除内容进入回收站，可恢复；恢复时恢复其删除前的全部关系（评
  论、点赞、转发、书签、收藏、修订、分类标签关联）。
- 14 天到期自动永久删除；任何人在到期前都不能提前永久删除（包括作者
  与管理员）。
  理由：用户选择仅到期自动清除，使恢复期对一切角色绝对有效。
- 永久删除级联删除该内容的评论、点赞、转发、书签、收藏、修订与媒体
  链接；媒体库中的媒体文件本身永不自动删除，仅可由用户手动删除。
  理由：媒体可能被多处引用，且用户明确媒体文件不做自动清理。
- 取消发布时用户可选择"存为草稿"（转为 Draft）或"丢弃"（进入软删
  除，仍走 14 天恢复期）。

### 6.6 媒体引用（MediaReference）

各聚合的媒体统一为对媒体库（§16）媒体项的引用；内容不内嵌媒体文
件。`MediaReference` 包含媒体库身份与可选的、随使用场景变化的
`AltText`。`AltText` 属于引用，因为同一媒体项在不同内容中可能需要
不同描述；提供时必须非空且不得包含首尾空白。

## 7. Blog

### 7.1 身份与路由

- `BlogIdentity`：全局唯一 Guid 内部身份。
- `BlogSlug`：系统生成、不可变、仅数字、保留前导零的字符串；账户内
  唯一（按 `AuthorId` 范围）；每账户单调递增分配；最小宽度 9 位
  （`000000001`），数字空间耗尽后扩展为更宽位数；永久删除后 slug 永
  不重用。
  理由：保留前导零要求 slug 建模为受验证的字符串而非整数；永不重用
  防止旧链接指向新内容；分配涉及跨聚合唯一性，由 Blog 聚合外部的协
  调者分配后传入。
- 公开路由形态：`/@{stringId}/blog/{slug}`。`@` 仅为 URL 展示层前
  缀，不进入领域值、DTO 或持久化（与 §3 前缀规则一致）。
- StringId 变更后，含旧值的 Blog 路由在旧值预留期（§3，默认 90 天）
  内 301 重定向到新路由；预留期满后返回 404。
  理由：预留期内旧身份仍属该账户，重定向保住既有链接；期满后旧身份
  可被他人占用，不得再重定向。

### 7.2 结构

```text
Blog
  Id: BlogIdentity
  AuthorId: UserIdentity
  Slug: BlogSlug
  Title: string (≤256)
  Blocks: 有序 ContentBlock 集合 (≤8192)
  Summary: string? (≤2048)
  FeaturedMediaId: MediaReference?
  Visibility: ContentVisibility
  CategoryId: CategoryIdentity?   // 可选，最多一个
  TagIds: BlogTagIdentity 集合 (≤32)
  CommentsAllowed: bool
  CommenterPolicy: CommenterPolicy
  Pinned: bool
  Featured: bool
  SeoTitle: string? (≤128)
  SeoDescription: string? (≤512)
  CanonicalUrl: string?
  CoAuthors: CoAuthor 集合 (≤32)
  Publication
  Deletion?
  CreatedAt / UpdatedAt
```

- 置顶（Pinned）与精选（Featured）是两个独立标记。
- 合著者（CoAuthor）为展示用标记，形态两者皆可：系统内账户引用
  （UserIdentity）或自由文本（≤64）；Blog 的所有权与权限仍只属于
  `AuthorId`。
  理由：不允许站点协作（§7.4），合著者仅是署名展示，不产生权限。
- 草稿不变量：标题必须非空；正文（块集合）可为空——配合自动保存。
  理由：用户决定仅正文可为空；标题非空使草稿在列表中可辨识。
- 发布不变量：发布/送审时仅校验标题非空；正文（块集合）可为空。
  理由：用户明确 Blog 发布不要求正文非空，与 Post/Moment 的"正文或
  媒体至少其一"规则区分。

### 7.3 修订历史

- 每次显式保存/发布生成一条不可变修订，全部版本永久保留，可回滚到
  任意版本。自动保存不生成修订，仅覆盖当前工作副本。
  理由：autosave 触发频繁，逐次生成修订会淹没真正有意义的历史。
- Blog 修订是独立的不可变记录，而非 Blog 聚合内部的集合。它包含强类
  型修订身份、所属 Blog 身份、有序内容块快照、创建时间，以及原因：手
  动保存、发布或回滚。修订仅快照正文块；标题、摘要、分类法、可见性、
  SEO 字段及其他元数据不进入修订。
- 显式保存与发布即使正文与最新修订相同也会创建修订，因为修订同时记录
  该显式动作。发布修订由审核通过与定时自动发布创建；送审、取消定时、
  退回草稿、取消发布不创建修订。
- 回滚先创建一条包含当前正文的回滚修订，再用所选历史快照替换工作正
  文；不复制所选目标修订本身。
- 已发布内容原地编辑、立即生效，同时记录修订。
  理由：用户确认完整修订历史 + 原地生效；修订是历史档案，不是待发
  布的平行版本。

### 7.4 协作与审核

- 不允许其他账户协作管理账户站点：站点内容的作者即站点所有者，无站
  点级成员/编辑角色模型。
- 发布状态集包含 `PendingReview`，作为单作者的编辑工作流状态（自我
  送审/待发布整理）。

### 7.5 批量操作与预览

- 支持批量操作（一次对多篇 Blog）：批量分类/标签赋值、批量可见性修
  改、批量状态操作（发布/取消发布/删除）。
- 预览不可分享：仅作者本人登录后可预览，不签发预览令牌。

## 8. Page

独立聚合，承载 About、Contact、隐私政策等非时间序站点页面。

- `PageIdentity`：全局唯一 Guid 内部身份。
- `PageRouteIdentifier`：可变，1–128 位 ASCII 字母、数字或 `-`；必须以
  字母或数字开头与结尾，禁止连续 `-`。保留所选大小写，唯一性与查找使
  用小写规范化值。唯一性按 `AuthorId` 范围，大小写不敏感。
- 域名与路由前缀不进入 `PageRouteIdentifier`。Application 路由将账户页
  面映射为社区路由 `/@{stringId}/{pageId}`，并可额外将所配置的 Personal
  根账户页面暴露为 `/{pageId}`。此种分离也使未来经验证的自定义域名可
  暴露同一 Page，而无需改变其领域身份。
- blog、post、moment 及分类法路由等系统保留路径由 Application 路由策略
  统一检查，而非硬编码于 Page 值对象中。
- 实质性路由变更创建独立的 `PageRouteReservation`，默认预留期单独可配
  置为 90 天；释放时间在变更时固定。预留期内旧标识对该账户内所有
  Page（含原 Page）均不可用；当目标 Page 可公开路由时以 301 重定向解
  析。仅改大小写只更新展示形态，不创建预留。
- 软删除保留当前路由与既有预留；删除期间公开路由不可用，恢复后恢复可
  用。永久删除立即释放当前路由；历史路由预留保持其已固定的释放时间并
  正常到期。
- 结构：标题（非空，≤256）、有序内容块集合（与 Blog 同模型且允许为
  空）、可见性、评论开关与评论者范围、SEO 标题/描述、特色媒体、发布、
  删除，以及创建/更新时间。
- 简化生命周期：仅 `Draft ⇄ Published` + 软删除（14 天回收规则与 §6.5
  相同）；无定时发布、无 PendingReview、无修订历史。首次与最近发布时间
  在取消发布后仍保留，并在 Page 重新发布时更新。
  理由：Page 变更频率低，用户明确选择简化生命周期，同时保留 Sitemap
  及其他输出投影所需的发布元数据。
- 每账户 Page 数量 ≤1024；跨聚合计数由 Application 协调者强制执行。

## 9. Post

Twitter 风格的媒体短帖。

```text
Post
  Id: PostIdentity
  AuthorId: UserIdentity
  Body: ContentBody?           // ≤8192
  Media: 只读 MediaReference 集合  // 图片+视频+音频，≤9
  Visibility: ContentVisibility
  AudienceRestrictionMode: Blacklist / Whitelist
  AudienceAccounts: UserIdentity 集合 (≤2048)
  QuotedPostId: PostIdentity?
  CommentsAllowed: bool
  CommenterPolicy: CommenterPolicy
  TagIds: PostTagIdentity 集合 (≤32)
  Publication
  Deletion?
  CreatedAt / UpdatedAt
```

- 草稿不变量：正文与媒体至少其一（不允许完全空的草稿）。
- 发布不变量：正文与媒体至少其一。引用不豁免——引用帖也必须自带正
  文或媒体（用户裁决，撤回裸引用）。
- 正文存在时，其源文本必须至少包含一个非空白字符；调用方应使用
  `null`，而非存储空正文对象。除此之外源文本原样保留。已发布 Post 的
  正文、媒体集合与引用目标不可变；可见性、受众限制、评论设置与标签仍
  可变；取消发布回到 Draft 后再次允许编辑正文与媒体。
- 同一 Post 内媒体身份必须唯一，即使各引用的 alt text 不同。Post 聚合
  强制集合形态与九项上限；Application 通过加载媒体记录校验被引用媒体
  属于作者且类型为图片、视频或音频。
- 受众限制是叠加在可见性枚举之上的可选账户集合。`Blacklist` 模式先按
  可见性确定受众，再从中排除指定账户；`Whitelist` 模式将可见性受众与
  指定账户取交集。两种模式都不能扩大可见性枚举授予的受众范围。
- 黑名单模式下空账户集合不产生限制；白名单模式下空账户集合产生空受众。
  账户集合最多包含 2048 个不重复的 `UserIdentity`，且不得包含作者；作
  者自身访问不受该叠加限制影响。
- 纯转发（Repost）建模为独立关系 `PostRepost`，不是含复制正文的特
  殊 Post；可转发自己的 Post；同一账户可多次转发同一 Post，因此每次
  转发都有独立的强类型身份。
- 引用（Quote）是一条新 Post：自带正文/媒体 + `QuotedPostId`；
  Post 只能转发/引用 Post。允许引用自己的 Post。
- 仅 Public 内容可被转发/引用；对非公开内容的转发/引用操作由领域直
  接拒绝。此处 Public 指活跃且已发布、`ContentVisibility.Public`、且无
  受众收窄的 Post：即 `Blacklist` 且账户集合为空。非空黑名单或任何白
  名单均使该 Post 不可被转发/引用，防止新内容绕过原受众限制。
  理由：用户决定在操作入口拦截，避免受限内容经转发链条外泄。
- 原 Post 被删除、隐藏或可见性降低时：引用保留自身内容，原内容位置
  显示"不可用"占位；纯转发整条隐藏；原内容恢复后引用与转发自动复
  原。
- 评论数、点赞数、转发数、收藏数为读投影。
- 标签使用独立的 PostTag 命名空间（§14）。

## 10. Moment

微信朋友圈风格的轻量分享。与 Post 刻意保持独立聚合：各自的身份、关
系、评论、命令、策略与仓储，使行为可独立演化。

```text
Moment
  Id: MomentIdentity
  AuthorId: UserIdentity
  Body: ContentBody?           // ≤2048
  Media: 只读 MediaReference 集合  // 图片+视频，≤9
  Visibility: ContentVisibility
  AudienceRestrictionMode: Blacklist / Whitelist
  AudienceAccounts: UserIdentity 集合 (≤2048)
  Location: 结构化位置 (坐标 + 地点名称)?
  QuotedMomentId: MomentIdentity?
  CommentsAllowed: bool
  CommenterPolicy: CommenterPolicy
  TagIds: MomentTagIdentity 集合 (≤32)
  Publication
  Deletion?
  CreatedAt / UpdatedAt
```

- 草稿与发布不变量与 Post 相同：正文与媒体至少其一。位置与引用内容不
  豁免。正文存在时，其源文本必须至少包含一个非空白字符；调用方应使用
  `null` 而非空正文对象，除此之外源文本原样保留。
- 已发布 Moment 的正文、媒体集合、位置与引用目标不可变；可见性、受众
  限制、评论设置与标签仍可变；取消发布回到 Draft 后再次允许编辑内容。
- 同一 Moment 内媒体身份必须唯一，即使各引用的 alt text 不同。Moment
  聚合强制集合形态与九项上限；Application 校验每项被引用媒体属于作者
  且类型为图片或视频。
- 可选位置包含必填的十进制纬度与经度，以及可选地点名称。纬度范围
  [-90, 90]，经度范围 [-180, 180]；地点名称若提供则必须非空白、不含首
  尾空白，且最多 128 字符。海拔、精度与外部地图服务商标识刻意不建模。
- 受众限制是叠加在可见性枚举之上的可选账户集合。`Blacklist` 模式先按
  可见性确定受众，再从中排除指定账户；`Whitelist` 模式将可见性受众与
  指定账户取交集。两种模式都不能扩大可见性枚举授予的受众范围。
- 黑名单模式下空账户集合不产生限制；白名单模式下空账户集合产生空受众。
  账户集合最多包含 2048 个不重复的 `UserIdentity`，且不得包含作者；作
  者自身访问不受该叠加限制影响。
- 转发是独立的 `MomentRepost` 关系，具有独立的强类型身份。Moment 只能
  转发/引用 Moment；允许引用或转发自己的 Moment，同一账户可多次转发同
  一 Moment。Moment 无收藏、无书签。
- 仅活跃且已发布、`ContentVisibility.Public`、且无受众收窄的 Moment 可
  被转发/引用：即 `Blacklist` 且账户集合为空。非空黑名单、任何白名单，
  或作者之间存在屏蔽时，领域直接拒绝该操作。
- 评论/点赞可见性规则与 Post 相同；原 Moment 不可用时转发/引用行为
  与 Post 相同（引用保留自身内容+占位，纯转发隐藏）。
- Moment 无故事式自动过期。软删除遵循共享的 14 天恢复规则（§6.5），到
  期后可永久清除。
- 标签使用独立的 MomentTag 命名空间（§14）。

## 11. 评论

- 按宿主分设独立评论聚合：`BlogComment`、`PostComment`、
  `MomentComment`、`PageComment`。
  理由：与内容聚合拆分决策一致，使各类评论可独立演化。
- 结构：`Id`、`AuthorId`（评论者为系统账户）、宿主强类型身份、可选
  `ParentCommentId`（嵌套深度不限）、`Body: ContentBody`（≤4096，支
  持 Markdown 与纯文本）、媒体（仅图片，≤4）、状态、CreatedAt。
  理由：评论者为系统账户，因为"每个评论者首次需审核"策略要求稳定识
  别评论者身份。
- 能力：Markdown、媒体、点赞；不可编辑。
- 状态机：`Pending` / `Approved` / `Spam` / `Deleted`；`Deleted` 不
  可恢复，仅保留占位记录以维持嵌套树结构。
  理由：深度不限的嵌套树下，硬删除会破坏子评论的上下文。
- 审核策略（三选一）：无需审核 / 每个评论者首次需审核（通过后该评
  论者后续评论直接显示）/ 所有评论都需审核。配置层级：站点级默认
  （`AccountSite`）+ 内容项覆盖。
- 每个内容项可关闭评论（`CommentsAllowed=false` 时所有人都不能评
  论，含作者本人）；每个内容项可设评论者范围
  （`CommenterPolicy`）：所有可阅读者 / 仅关注者 / 仅互相关注 / 仅
  作者；默认"所有可阅读者"。
  理由：用户选择比阅读权限更严格的评论控制能力，两级控制（开关 +
  范围）互不替代。
- 评论计数为读投影；评论不存放于宿主聚合内。

## 12. 点赞

- 独立点赞关系：`BlogLike`、`PostLike`、`MomentLike`；评论点赞为评
  论聚合对应的独立关系。
- 点赞者身份不公开：任何人均只见点赞计数（仅计数公开）。
  理由：用户选择仅计数公开，因此无需按可见性过滤点赞者列表。
- 软删除内容恢复后，其点赞一并恢复（§6.5）。

## 13. Blog 收藏与 Post 书签

收藏仅属 Blog，书签仅属 Post；Moment 两者皆无。

```text
BlogFavorite / BlogFavoriteFolder
PostBookmark / PostBookmarkFolder
```

- 文件夹私有，仅本人可见。
- 文件夹名称：每账户内唯一、大小写敏感、≤128；每账户文件夹 ≤512。
- "未分类"用可空文件夹身份表示，不设特殊的用户可编辑文件夹记录；删
  除文件夹时其条目移入未分类。
- 文件夹与文件夹内条目均可手动排序。
- 收藏/书签唯一性按账户判定（同一账户不可重复收藏同一目标）。
- 已删除或不可访问的目标在文件夹中显示为"不可用条目"，恢复后自动
  复原。
  理由：用户选择保留占位而非自动移除，避免内容暂时不可用时丢失收
  藏组织。

## 14. 分类与标签

- 分类（Category）与标签（Tag）均为每账户独立拥有，非全局分类法；
  各自有稳定身份（全局唯一 Guid）使用户改名不破坏内容关联；内部关
  联一律使用 Guid。
- 路由标识：用户指定的文本标识，可变，账户内唯一；显示名称与路由标
  识分离，均不进入内部关联。
- 名称：≤64，每账户唯一，大小写敏感。
- 分类：扁平（无层级）；一篇 Blog 的分类可选且最多一个；每账户分类
  ≤2048。
- 标签：Blog/Post/Moment 使用各自独立的标签命名空间
  （BlogTag/PostTag/MomentTag）；每账户标签 ≤8192；单个内容项标签
  ≤32。
- 标签附加元数据：描述（≤1024）、封面媒体（媒体库引用）、SEO 元数
  据（SEO 标题 ≤128 / SEO 描述 ≤512）、显示顺序（可手动排序）。
- 分类/标签归档页为读投影（§17）。

## 15. 导航菜单

- 用户管理的多菜单：每账户菜单 ≤64，每菜单菜单项 ≤64。
- 菜单项目标可为：内部路径、外部 URL、Page 页面、分类/标签归档页。
- 菜单项有序，支持手动排序。

## 16. 媒体库

媒体库是一等聚合，集中管理账户上传的媒体；Blog、Post、Moment、
Page、评论及 AccountProfile 的媒体统一引用媒体库媒体项。

- 媒体项按账户隔离。
- 媒体文件永不自动删除（内容永久删除时仅删除引用链接），仅用户手动
  删除。
- Post 媒体：图片/视频/音频，≤9；Moment 媒体：图片/视频，≤9；评论
  媒体：仅图片，≤4。

## 17. 输出与发现

- RSS/Atom、Sitemap、归档页：动态 API 与静态 JSON 产物均提供；归档
  页维度：日期、分类、标签、作者（均为读投影）。
- 搜索：仅动态 API 提供，不进入静态产物；范围为全部内容类型
  （Blog/Post/Moment/Page）。
- `TimelineEntry` 统一读投影聚合所有内容类型的时间线。
- 输出功能的启用由 `AccountSite` 输出开关控制。
- 可见性鉴权在 Application 策略执行，并在公共查询边界再次执行；静
  态生成同样只输出公开内容。

## 18. 明确排除项

以下能力经决策不进入当前设计：

- Archived 生命周期状态（归档页是读投影）。
- 裸引用（引用必须自带正文或媒体）。
- 站点协作与站点级编辑角色；`PendingReview` 仅服务单作者工作流。
- 页面模板、本地化/翻译变体、导入导出、预览分享令牌。
- 评论编辑、评论 Deleted 恢复。
- 自定义域名（仅延后，未来需要时再设计）。
- 共享可变内容基类、大型通用 `ContentItem` 聚合。

## 19. 约束速查表

| 项目 | 上限 |
| --- | --- |
| Post 正文 | 8192 字符 |
| Moment 正文 | 2048 字符 |
| 评论正文 | 4096 字符 |
| Blog/Page 文本型内容块（文本/引文/代码） | 2097152 字符 |
| Blog 标题 | 256 字符 |
| Blog 摘要 | 2048 字符 |
| SEO 标题 | 128 字符 |
| SEO 描述 | 512 字符 |
| 标签/分类名称 | 64 字符 |
| 标签描述 | 1024 字符 |
| Page 路由标识 | 128 字符 |
| 昵称 / 站点标题 / 合著者文本 | 64 字符 |
| 简介 | 2048 字符 |
| 站点描述 | 1024 字符 |
| 位置 | 128 字符 |
| 文件夹名 | 128 字符 |
| 内容块/篇 | 8192 |
| 标签/内容项 | 32 |
| Post 媒体 | 9（图/视/音） |
| Moment 媒体 | 9（图/视） |
| 评论媒体 | 4（仅图） |
| 媒体单文件 | 图 16 MB / 音 64 MB / 视 1 GB |
| 媒体总配额 | 按部署配置 |
| 受众限制账户 | 2048 账户 |
| 合著者 | 32 |
| 文件夹/账户 | 512 |
| 标签/账户 | 8192 |
| 分类/账户 | 2048 |
| 菜单/账户 | 64 |
| 菜单项/菜单 | 64 |
| Page/账户 | 1024 |
| 软删除恢复期 | 14 天（固定） |
